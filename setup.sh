#To install virtual environment: 
python -m venv venv

#Install requirements
venv/bin/pip install gitpython pystray Pillow==9.5.0

#For it to work well I used to install this in the system
apt install python-tk